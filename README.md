# Davysoft — Odoo addons

Modules Odoo tiers/adaptés retenus et maintenus par Davysoft, un dossier par module.

Chaque module importé depuis un dépôt tiers via `git subtree` conserve son historique d'origine — voir le message du commit d'import pour la source amont.
